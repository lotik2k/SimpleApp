<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <TextView
        android:id="@+id/textView"
        android:layout_width="0dp"
        android:layout_height="68dp"
        android:background="@color/grey_color"
        android:fontFamily="@font/roboto_bold"
        android:text="Выберите тариф"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:textColor="@color/text_color_black"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <!--<ListView
        android:id="@+id/dialog_passengers_comfort_types"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_dialog_passengers_comfort_type"
        app:layout_constraintTop_toBottomOf="@+id/textView">
    </ListView>-->

    <RadioGroup
        android:id="@+id/comfort_types_radio_group"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:divider="?dividerHorizontal"
        android:showDividers="middle"
        app:layout_constraintTop_toBottomOf="@+id/textView">

        <RadioButton
            android:id="@+id/first_comfort_type"
            style="@style/PassengersComfortTypesButton"
            tools:text="Эконом" />

        <RadioButton
            android:id="@+id/second_comfort_type"
            style="@style/PassengersComfortTypesButton"
            tools:text="Эконом" />

        <RadioButton
            android:id="@+id/third_comfort_type"
            style="@style/PassengersComfortTypesButton"
            tools:text="Эконом" />

        <RadioButton
            android:id="@+id/fourth_comfort_type"
            style="@style/PassengersComfortTypesButton"
            tools:text="Эконом" />
    </RadioGroup>

</android.support.constraint.ConstraintLayout>